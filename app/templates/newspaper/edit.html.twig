{% include 'default/_form.html.twig' with {
    page_title: 'title.newspaper_edit'|trans({'%id%': newspaper.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'newspaper_index',
} %}